# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Fixed an issue on escrow file where job was not checked when clicking / using an apiary object to be placed.
2. Improved the length of the uniqueId when placing an apiary.

## Modified Files:

- server/main.lua
- server/escrow_ignore.lua
- fxmanifest.lua

## Notes:

- Add on escrow file manually (if you have added your own code / otherwise replace), the following check by checking out the `server/escrow_ignore.lua` file where this code has been placed.
  
```lua
local hasRequiredJob = HasRequiredJob(API.getJob(_source)) -- 2.0.9

if not hasRequiredJob then -- 2.0.9
    SendNotification(_source, 'NOT_REQUIRED_JOB', Locales['NOT_REQUIRED_JOB'].message, 'error')
    return
end
```

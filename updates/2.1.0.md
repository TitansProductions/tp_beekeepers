# What has been updated?

## Changelog - (Added, Fixed, Removed):

1. Improved the generation of the new placed apiaries by using the AUTO_INCREMENT value on `id` database table.

## Modified Files:

- server/main.lua
- fxmanifest.lua

## Notes

- Use this on database where `query` is to modify the `id` on `tp_beekeepers` database table.

```sql
ALTER TABLE `tp_beekeepers`
MODIFY COLUMN `id` int(11) NOT NULL;
```
